# 久吾動物醫院排班表　部署與設定說明

本文件記錄網站與資料庫的所有設定，供日後維護或更換負責人時參考。一般使用方式請看 `README.md`。

## 一、整體架構

| 項目 | 服務 | 位置 |
| --- | --- | --- |
| 程式碼 | GitHub 倉庫 | https://github.com/Nastya-777/JIUWU |
| 網站（給員工開啟的網址） | GitHub Pages | https://nastya-777.github.io/JIUWU/ |
| 資料（員工、排班表） | Firebase Realtime Database | https://jiu-wu-default-rtdb.asia-southeast1.firebasedatabase.app |

流程：程式碼推送到 GitHub 倉庫 → GitHub Actions 自動把網頁發布到 GitHub Pages → 員工開啟網址 → 網頁直接讀寫 Firebase 資料庫。三者都使用免費方案，不會產生費用。

## 二、GitHub

### 2.1 基本資訊

- 帳號：`Nastya-777`
- 倉庫：`JIUWU`（公開）
- 預設分支：`claude/jiuwu-animal-hospital-schedule-hcnsp7`
- 自動發布流程：`.github/workflows/pages.yml`，名稱「Publish schedule site」

### 2.2 已完成的一次性設定

1. 倉庫改為公開：Settings → General → Danger Zone → Change repository visibility → Make public。
   免費方案的私人倉庫無法使用 GitHub Pages，改回私人網站就會失效。
2. 開啟 Pages：Settings → Pages → Build and deployment → Source 選「GitHub Actions」。

### 2.3 如何更新網站

任何推送到預設分支的變更都會自動重新發布，約一分鐘生效。兩種修改方式：

- **在 GitHub 網頁上直接改**：打開檔案 → 右上角鉛筆圖示 → 修改 → Commit changes。
- **在電腦上改**：修改後 `git push`。

查看發布狀態：倉庫頁面 → Actions 分頁 → 最上面一筆「Publish schedule site」，綠色勾勾代表成功。

手動重新發布：Actions → 左側「Publish schedule site」→ Run workflow → Run workflow。

### 2.4 檔案說明

```
index.html          網頁本體
config.js           設定：資料存在本機或 Firebase（見第四節）
css/style.css       外觀樣式
js/scheduler.js     排班演算法
js/storage.js       資料儲存層（本機 / Firebase）
js/app.js           介面、匯出
vendor/             匯出 PDF / PNG / XLSX 用的第三方函式庫
test/               演算法測試（node test/scheduler.test.js）
.github/workflows/  自動發布流程
README.md           使用說明
```

## 三、Firebase

### 3.1 基本資訊

- 控制台：https://console.firebase.google.com/project/jiu-wu/overview
- 專案名稱：JIU WU，專案 ID：`jiu-wu`
- 登入帳號：建立專案時使用的 Google 帳號
- 方案：Spark（免費，未綁信用卡，不會自動扣款）
- 資料庫類型：Realtime Database（不是 Firestore）
- 資料庫位置：asia-southeast1（新加坡）
- 資料庫網址：`https://jiu-wu-default-rtdb.asia-southeast1.firebasedatabase.app`
- 資料存放路徑：`jiuwu_schedule`

### 3.2 安全規則（必做）

建立資料庫時選的「測試模式」規則只有 30 天有效，到期後所有人都無法讀寫。請改成下列不會過期的規則：

1. 控制台左側「数据库和存储」→ Realtime Database → 上方「规则」分頁。
2. 全部刪除，貼上：

```json
{
  "rules": {
    "jiuwu_schedule": { ".read": true, ".write": true }
  }
}
```

3. 按「发布」。

這組規則只開放 `jiuwu_schedule` 路徑，資料庫其他部分一律拒絕。意思是：知道網址的人都能讀寫排班資料，因此請不要把網址公開張貼，只給院內人員。

### 3.3 資料長什麼樣

控制台 → Realtime Database → 「数据」分頁可以直接看到與手動修改：

```
jiuwu_schedule
├─ employees          員工清單（id、name、deleted）
├─ settings           每日出勤人數 perDay
└─ months
   ├─ 2026-09
   │   ├─ working     目前顯示的內容（含未儲存的修改）
   │   ├─ saved       上次按「儲存」的內容
   │   └─ savedAt     儲存時間
   ├─ 2026-10
   └─ ...
```

- `phase` 為 `draft` 代表還在選擇休假，`generated` 代表已生成排班表。
- 儲存格代碼：`W` 上班、`R` 休息、`S` 特休。
- 刪除員工只是標記 `deleted: true`，往期排班表仍會顯示。

### 3.4 備份與清空

- 備份：網頁底部「備份資料」會下載完整 JSON；或在控制台「数据」分頁右上角「⋮」→「导出 JSON」。
- 還原：網頁底部「還原資料」選擇備份檔。
- 清空：控制台「数据」分頁，滑鼠移到 `jiuwu_schedule` 上按「×」刪除，網頁重新整理後就是全新狀態。

### 3.5 免費額度

| 項目 | 免費額度 | 排班表實際用量 |
| --- | --- | --- |
| 儲存空間 | 1 GB | 一年不到 1 MB |
| 每月下載流量 | 10 GB | 每次開頁約 50 KB |
| 同時連線數 | 100 | 同時開著頁面的人數 |

用量可在控制台 Realtime Database →「用量」分頁查看。超過額度只會暫停服務，不會收費。

### 3.6 更換資料庫

若要換到另一個 Firebase 專案，或換一個存放路徑：修改 `config.js` 的 `databaseURL` 與 `path`，推送後網站自動更新。舊資料不會自動搬移，請先備份再還原。

## 四、config.js

```js
window.JIUWU_CONFIG = window.JIUWU_CONFIG || {
  storage: 'firebase',   // 'firebase' 共用資料庫；'local' 只存在各自裝置
  firebase: {
    databaseURL: 'https://jiu-wu-default-rtdb.asia-southeast1.firebasedatabase.app',
    path: 'jiuwu_schedule'
  }
};
```

把 `storage` 改成 `'local'` 就會回到各裝置獨立儲存的模式。

## 五、常見問題

**右上角顯示「共用資料庫：連線中斷」**
網路不通，或 Firebase 規則已過期（見 3.2）。此時修改會暫存在該裝置，連線恢復後自動補寫。

**網站沒有更新**
先重新整理頁面。仍未更新則到 GitHub → Actions 看最新一筆是否成功；失敗時點進去看紅色的步驟。

**之前在某台裝置輸入的資料不見了**
那是切換到共用資料庫前存在該裝置的資料。在該裝置用「備份資料」下載，再用「還原資料」匯入即可。

**想讓網址不含 github.io**
可購買網域，在 GitHub → Settings → Pages → Custom domain 設定；免費方案也支援。

---

© 2026 KE FEI. All rights reserved.
